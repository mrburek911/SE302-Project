{
  "name": "ekupi-playwright-pom-smoke",
  "version": "1.0.0",
  "private": true,
  "description": "Smoke test suite for ekupi.ba using Playwright + Page Object Model (TypeScript)",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke --reporter=line",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "test:functional": "playwright test tests/functional --reporter=line",
    "test:all": "playwright test --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "typescript": "^5.6.3"
  }
}